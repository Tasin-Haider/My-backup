#include <stdio.h>
#include <stdlib.h>
#include <time.h>

struct Block {
    size_t count;
    size_t capacity;
    int arr[];
};

struct *Block create_array(size_t capacity){

}

int main(void) {

    return 0;
}
